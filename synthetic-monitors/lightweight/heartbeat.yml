heartbeat.monitors:
  - type: http
    name: Storefront Homepage Lightweight
    id: storefront-lightweight
    enabled: true
    urls: "http://ingress-nginx-controller.ingress-nginx.svc.cluster.local"
    schedule: "@every 1m"
    timeout: 16s
    alert.status.enabled: true
